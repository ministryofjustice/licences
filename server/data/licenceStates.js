

const ALL_STATES = ['UNSTARTED', 'STARTED', 'SENT'];

const REQUIRED_STATES = ['UNSTARTED', 'STARTED'];

const SENT_STATES = ['SENT'];

module.exports = {
    ALL_STATES,
    REQUIRED_STATES,
    SENT_STATES
};
