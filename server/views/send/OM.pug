extends ../layout

block content

    h1.heading-large Send to the prison OMU team
    p The licence is ready to be sent to the OMU team or you can return to your dashboard and send it later.

    div#continueBtns.inlineButtons
        form#acceptanceForm(method='POST', action='/send/omu')
            input(type="hidden" name="_csrf" value=csrfToken)
            input(type="hidden" name="nomisId" value=nomisId)
            input#continueBtn.requiredButton.button(type="submit" value="Send")
        a#backBtn.requiredButton.button.button-secondary(href="/" role="button") Back to dashboard
