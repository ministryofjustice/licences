h2.heading-medium.borderBottomLightThick.separatorTitle Additional conditions

-var conditions = licence.licenceConditions

for condition, index in conditions
    div.pure-g.borderBottomLight.midPaddingTopBottom
        div.pure-u-2-3.additional
            if condition.group
                div.heading-small.noMargin.smallPaddingBottom Condition #{index + 1}
                div.heading-small.noMargin.smallPaddingBottom #{condition.group}
                    if condition.subgroup
                        |  -- #{condition.subgroup}

            div.additionalContent
                for item in condition.content
                    if item.text
                        | #{item.text}
                    if item.variable
                        span(class="bold") #{item.variable}

        div.pure-u-1-3.alignRight
            if(index == 0 && user.role === 'RO')
                a.link(href="/hdc/licenceConditions/additionalConditions/" + nomisId) Change these details




