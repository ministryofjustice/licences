div.largeMarginBottom.borderTop
    h2.heading-medium Postponement

    div.borderBottomLight.largeMarginBottom

    div.pure-g
        div.pure-u-3-4
            if licenceStatus.decisions.postponed
                | Licence postponed
            else
                | Use this to indicate that the process ispostponed while any risk management planning actions remain outstanding
        div.pure-u-1-4
            a.taskListAction.center.button(href="/hdc/reporting/" + prisonerInfo.offenderNo)
                if licenceStatus.decisions.postponed
                    | Resume
                else
                    | Postpone
