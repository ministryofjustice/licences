div.largeMarginBottom.borderTop
    h2.heading-medium Additional conditions

    div.borderBottomLight.largeMarginBottom

    div.pure-g
        div.pure-u-9-12
            if !additionalConditions
                | No conditions added
            else
                | Additional conditions added
        div.pure-u-1-4
            a.taskListAction.center.button(href="/hdc/additionalConditions/" + prisonerInfo.offenderNo)
                if !additionalConditions
                    | Start
                else
                    | View
