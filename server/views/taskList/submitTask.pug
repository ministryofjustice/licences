div.largeMarginBottom.borderTop
    h2.heading-medium Submit to OMU

    div.borderBottomLight.largeMarginBottom

    div.pure-g
        div.pure-u-3-4
            if !taskData.readyToSubmit
                | Tasks not yet complete
            else
                | Ready to submit
        div.pure-u-1-4
            if taskData.readyToSubmit
                a.taskListAction.center.button(href="/hdc/send/" + prisonerInfo.offenderNo)
                    | Continue

