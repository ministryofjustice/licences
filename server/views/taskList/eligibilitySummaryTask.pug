div(class= user.role === 'DM' ? "taskListItem borderBottom largeMarginBottom" : "taskListItem")
    h2.heading-medium Eligibility and presumed suitability

    div.pure-g
        div.pure-u-3-4
            if licenceStatus.decisions.excluded
                | Excluded
            else if licenceStatus.decisions.unsuitable
                | Unsuitable
                if licenceStatus.decisions.exceptionalCircumstances
                    |  with exceptional circumstances
            else if licenceStatus.decisions.insufficientTime
                | Less than 4 weeks until release date
                if licenceStatus.decisions.insufficientTimeContinue
                    | , with DM approval to continue
            else
                | Eligible, presumed suitable, more than 4 weeks until release date
        div.pure-u-1-4.center
            if user.role === 'CA'
                a.taskListAction.center.button.button-secondary(href="/hdc/eligibility/excluded/" + prisonerInfo.offenderNo) View/Edit
            else
                a.taskListAction.center.button.button-secondary(href="/hdc/review/eligibility/" + prisonerInfo.offenderNo) View




