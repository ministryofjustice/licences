div.paddingBottom
    h2.heading-medium Create licence

    div.pure-g
        div.pure-u-1.pure-u-md-3-4
            | #{statusLabel}
        div.pure-u-1.pure-u-md-1-4.center
            if postApproval && licenceStatus.stage !== 'MODIFIED_APPROVAL'
                if user.role === 'CA' && licenceStatus.decisions.approved && licenceStatus.decisions.curfewAddressApproved !== 'withdrawn'
                    a#createPdf.taskListAction.center.button(href="/hdc/pdf/select/" + bookingId role="button") Continue




