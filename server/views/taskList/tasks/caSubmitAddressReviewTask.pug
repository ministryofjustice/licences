div.largeMarginBottom

    h2.heading-medium Submit to responsible officer

    div.pure-g

        if licenceStatus.decisions.optedOut
            div.pure-u-1.pure-u-md-3-4
                | Submission unavailable - Offender has opted out of HDC

        else
            div.pure-u-1.pure-u-md-3-4
                if ['STARTED', 'DONE'].includes(licenceStatus.tasks.curfewAddress)
                    | Ready to submit
                else
                    | Not completed

            div.pure-u-1.pure-u-md-1-4.center
                if ['STARTED', 'DONE'].includes(licenceStatus.tasks.curfewAddress) && !licenceStatus.decisions.optedOut
                    a#continueBtn.taskListAction.center.button(href='/hdc/review/curfewAddress/' + prisonerInfo.bookingId)
                        | Continue
